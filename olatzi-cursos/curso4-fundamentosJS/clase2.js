

var nombre = 'Diana', apellido = 'Ledesma';

var nomMayusculas = nombre.toUpperCase(); 
var apellidpMinusculas = apellido.toLowerCase();
var letra = nombre.charAt(2);
var tamanio = nombre.length;

//interpolación,concatenacion
var nombreCompleto=`${nombre} ${apellido.toUpperCase()}`